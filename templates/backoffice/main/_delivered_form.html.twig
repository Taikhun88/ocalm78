<form method="post" action="{{ path('app_backoffice_order_delivered', {'id': commande.id}) }}" onsubmit="return confirm('Etes-vous sûr(e) que la livraison est confirmée ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delivered' ~ commande.id) }}">
    <button class="btn btn-primary mt-1">Confirmer la livraison</button>
</form>
