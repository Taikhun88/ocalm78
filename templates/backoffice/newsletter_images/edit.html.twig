{% extends 'layout.html.twig' %}

{% block title %}
	{{parent()}}
	- Editer l'image de newsletter
{% endblock %}

{% block content %}
	<h1>Editer l'image de newsletter</h1>

	{{ include('backoffice/newsletter_images/_form.html.twig', {'button_label': 'Valider'}) }}

	{{ include('backoffice/newsletter_images/_delete_form.html.twig') }}

	<a class="text-light" href="{{ path('app_backoffice_newsletter_images_index') }}">
		<i class="fas fa-reply-all"></i>
	</a>
{% endblock %}
