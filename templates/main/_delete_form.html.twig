<form method="post" action="{{ path('profil_delete', {'id': user.id}) }}" onsubmit="return confirm('{% trans %}Delete account ?{% endtrans %}');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user.id) }}">
	<button class="btn btn-danger">Supprimer</button>
</form>
