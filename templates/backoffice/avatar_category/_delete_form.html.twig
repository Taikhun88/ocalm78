<form method="post" action="{{ path('app_backoffice_avatar_category_delete', {'id': avatar_category.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ avatar_category.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
